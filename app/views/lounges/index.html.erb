
<h1>Lounges Index</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

  <% @lounges.each do |lounge| %>
  <tr>
    <td><%= link_to(lounge.name, airport_lounge_path(lounge.airport.id, lounge.id)) %></td>
  </tr>
  <% end %>
</table>
